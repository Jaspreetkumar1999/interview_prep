const length = 4;
const numbers = [];
for (var i = 0; i < length; i++){
  numbers.push(i + 1);
}
console.log(numbers);

function request(req, res, next){
   next()
}

{
    skip : 20
}

slect * from  tool 
offset 20 row

// debugger.que({}, (err, res) )